/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  head: {
    link: {
      rel: "icon",
      href: "/icons/favicon.ico",
    },
  },
};

module.exports = nextConfig;
